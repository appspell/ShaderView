<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appspell.shaderview.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShaderView">
        <activity android:name=".ui.UiActivity" />
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".video.VideoActivity" />
        <activity android:name=".video.VideoAdvancedActivity" />
        <activity android:name=".simple.SimpleShaderActivity" />
        <activity android:name=".simple.SimpleOnlyXMLShaderActivity" />
        <activity android:name=".list.ShaderListActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
    </application>

</manifest>